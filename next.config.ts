import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {  
    domains: ['images.unsplash.com','res.cloudinary.com'], // Add any other domains you need here  
  }, 
};

export default nextConfig;
